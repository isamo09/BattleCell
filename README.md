# BattleCell Security

Современное веб-приложение для безопасного хранения паролей и файлов с мастер-паролем.

## Возможности

- 🔐 **Трехуровневое шифрование** - Серверный ключ + случайный ключ шифрования + мастер-пароль
- 🔐 **Случайные ключи шифрования** - Уникальный 256-байтный ключ для каждого пользователя
- 🔐 **Мастер-пароль** - Шифрует пользовательский ключ, который в свою очередь шифрует данные
- 📁 **Защищенное файловое хранилище** - 500MB общее хранилище на пользователя
- 🎨 **Современный интерфейс** - темно-прозрачный дизайн
- 🔒 **Безопасность** - все данные шифруются случайным ключом шифрования
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Технологии

- **Backend**: Flask, SQLite, Cryptography
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Аутентификация**: Flask-Login
- **Шифрование**: Fernet (AES-256), PBKDF2

## Установка

### Требования

- Python 3.8+
- pip

### Шаги установки

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/isamo09/BattleCell
   cd supercell
   ```

2. **Создайте виртуальное окружение**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**
   
   **Windows:**
   ```bash
   venv\Scripts\activate
   ```
   
   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

5. **Настройте переменные окружения** (опционально)
   ```bash
   export SECRET_KEY="your-super-secret-key-here"
   ```

6. **Запустите приложение**
   ```bash
   python app.py
   ```

7. **Откройте браузер**
   ```
   http://localhost:5000
   ```

## Использование

### Регистрация и вход

1. Перейдите на главную страницу
2. Нажмите "Регистрация" и создайте аккаунт
3. После регистрации войдите в систему
4. Настройте мастер-пароль

### Настройка мастер-пароля

1. Войдите в аккаунт
2. При первом добавлении пароля или файла будет предложено установить мастер-пароль
3. Система автоматически генерирует пользовательский ключ (512 байт) и случайный ключ шифрования (256 байт)
4. Мастер-пароль шифрует пользовательский ключ, который в свою очередь шифрует все данные
5. Случайный ключ шифрования шифруется серверным ключом и хранится в отдельной таблице
6. Можно изменить мастер-пароль в настройках (пользовательский ключ будет перешифрован)

### Хранение паролей

1. Перейдите в раздел "Пароли"
2. Нажмите "Добавить пароль"
3. Заполните форму (название, пользователь, пароль)
4. Введите мастер-пароль для шифрования
5. Для просмотра пароля введите мастер-пароль

### Файловое хранилище

1. Перейдите в раздел "Файлы"
2. Нажмите "Загрузить файл"
3. Выберите файл (общий лимит 500MB на все файлы)
4. Введите мастер-пароль для шифрования
5. Для скачивания файла введите мастер-пароль

## Безопасность

### Шифрование

- **Трехуровневая система шифрования**:
  - Серверный ключ - шифрует случайные ключи шифрования пользователей
  - Случайный ключ шифрования (256 байт) - шифрует все данные пользователя
  - Пользовательский ключ (512 байт) - шифруется мастер-паролем
  - Мастер-пароль - шифрует пользовательский ключ
- **AES-256** для шифрования данных
- **PBKDF2** с 200,000 итераций для генерации ключей из мастер-пароля
- **Fernet** для шифрования ключей и данных
- **Мастер-пароль** никогда не сохраняется в открытом виде
- **Случайные ключи шифрования** хранятся в отдельной таблице `encryption_keys`

### Аутентификация

- **Мастер-пароль** (Единый ключ шифрования)
- **Безопасное хранение** всех данных
- **Перешифровка** при смене мастер-пароля

### Хранение данных

- **SQLite** база данных
- **Шифрованные BLOB** для файлов и паролей
- **Изолированные данные** для каждого пользователя
- **Таблица `encryption_keys`** для хранения зашифрованных ключей шифрования
- **Упрощенная таблица `user_settings`** без неиспользуемых настроек

## Структура проекта

```
supercell/
├── app.py                 # Основное Flask приложение
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
├── database.db           # SQLite база данных (создается автоматически)
├── uploads/              # Папка для загрузок (создается автоматически)
├── templates/            # HTML шаблоны
│   ├── base.html         # Базовый шаблон
│   ├── index.html        # Главная страница
│   ├── dashboard.html    # Дашборд
│   ├── passwords.html    # Управление паролями
│   ├── files.html        # Файловое хранилище

└── static/               # Статические файлы
    ├── css/
    │   └── style.css     # Стили
    └── js/
        └── main.js       # JavaScript
```

## API Endpoints

### Аутентификация
- `GET /` - Главная страница
- `GET /register` - Страница регистрации
- `POST /register` - Регистрация пользователя
- `GET /login` - Страница входа
- `POST /login` - Вход в систему
- `GET /logout` - Выход из системы

### Основные функции
- `GET /dashboard` - Дашборд пользователя
- `GET /passwords` - Список паролей
- `POST /add_password` - Добавление пароля
- `POST /get_password/<id>` - Получение пароля
- `GET /files` - Список файлов
- `POST /upload_file` - Загрузка файла
- `POST /download_file/<id>` - Скачивание файла


## Горячие клавиши

- `Ctrl+K` - Быстрое добавление (пароль/файл)
- `Escape` - Закрытие модальных окон
- `Ctrl+S` - Сохранение (в формах)

## Разработка

### Добавление новых функций

1. Добавьте маршрут в `app.py`
2. Создайте HTML шаблон в `templates/`
3. Добавьте стили в `static/css/style.css`
4. Добавьте JavaScript в `static/js/main.js`

### Тестирование

```bash
# Запуск в режиме разработки
python app.py

# Проверка синтаксиса
python -m py_compile app.py

# Проверка зависимостей
pip check
```

## Лицензия

MIT License

## Поддержка

Для вопросов и предложений создайте Issue в репозитории.

## Безопасность

Если вы обнаружили уязвимость безопасности, пожалуйста, сообщите об этом приватно.

---

**Внимание**: Это приложение предназначено для личного использования. Для продакшена рекомендуется дополнительная настройка безопасности.

